{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "catalog": {
      "$wineKey": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
